1) Porque un lenguaje de programacion compila a lenguaje ensamblador el cual es 
ejecutado por el sistema operativo, este lenguaje ensamblador depende de 2 coosas,
de la arquitectura del procesador y la interpretacion que le el sistema operativo.

2) GNU/Linux, Mac OS X, OS/2 Warp, Genode, Windows y Solaris/OpenSolaris.

3) Un hypervisor permite que un ordenador host preste soporte a varias maquinas virtuales invitadas mediante el uso de compartido virtual de sus recursos, como la memoria y el procesamiento

4) No, por que cada maquina virtual esta englobada en su propio 'mundo'